extends layout

block content
  h1= title

  ul
    each author in author_list
      li
        a(href=author.url) #{author.name}
        |  (birth date: #{author.date_of_birth_formatted} - death date: #{author.date_of_death_formatted})

    else
      li There are no authors.
